export interface CharacterFilter {
  status: string;
  gender: string;
  name: string;
}
export interface LocationFilter {
  name: string;
  type: string;
  dimension: string;
}

export interface EpisodeFilter {
  name: string;
  episode: string;
}
